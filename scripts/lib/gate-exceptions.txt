# ABI 门禁豁免清单
#
# 上游发行包里常夹带一些不在运行路径上的可选组件，它们的依赖无法满足，
# 但也不会被调用。为此改动上游包内容风险更大，故在此登记豁免。
#
# 登记的前提是能说清楚"为什么它不会被执行"。凡是在运行路径上的二进制，
# 一律不得登记 —— 那属于必须修复的问题。
#
# 格式：<路径子串> | <允许缺失的库> | <理由>

rabbitmq/lib/lib/odbc- | libodbc.so.2 | Erlang 附带的 ODBC 桥接程序，仅在调用 odbc 模块时启动；RabbitMQ 不使用 ODBC，该二进制不在运行路径上
