# 已知敏感值的 SHA-256 指纹
#
# 这些口令必须保留在构建产物中（现场部署依赖它们），但绝不能进入本仓库。
# 此处只存单向指纹：sha256 不可逆，公开无害，却能拦下任何试图提交明文的操作。
#
# 真实值由 secrets/package-secrets.yaml 提供（该目录已 gitignore），
# 构建时注入产物。参见 secrets/package-secrets.yaml.example。
#
# 新增一条：scripts/lib/secrets-scan.sh --add-fingerprint
#
# 格式：<sha256>  <用途标签>

72bad8191b02cf03c98fd95b56088089619399d7e2c1e604d136c96fb2d7b519  rabbitmq-default-password
